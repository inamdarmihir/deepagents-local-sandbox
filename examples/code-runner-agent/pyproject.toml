[project]
name = "code-runner-agent"
version = "0.1.0"
description = "Tutorial: deepagents + deepagents-local-sandbox code execution agent"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.5",
    "deepagents-local-sandbox[anthropic]>=0.1.0",
    "pyyaml>=6.0",
    "rich>=13.0",
]
